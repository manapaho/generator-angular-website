/**
 * This is the <%= yeoman.choices.feature %> module.
 * Only declare the module's dependencies in this file.
 * @namespace <%= yeoman.choices.Feature %> Module
 */
(function () {
  'use strict';

  // Create the module and declare its dependencies.
  angular.module('<%= yeoman.choices.module %>', [
      'app.core'
    ]
  );

})();
