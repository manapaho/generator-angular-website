/**
 * This is the home module configuration.
 * Do NOT configure routes in this file.
 * @memberOf Home Module
 * @namespace Home Config
 */
(function () {
  'use strict';

  // Register the module's configuration.
  angular.module('app.home').config(config);

  // Inject the dependencies.
  config.$inject = [];

  /**
   * @name config
   * @desc Configures the module.
   * @memberOf Home Module.Home Config
   */
  function config() {
  }

})();
